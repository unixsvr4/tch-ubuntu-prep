# Day 4 — Spot-the-Bug examples
# Purpose: run `make bugs-scan` and identify every flagged check.
# These are the exact bugs from tch_prep_claude0.md section 4.2.

terraform {
  required_version = ">= 1.5"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region                      = "us-east-1"
  access_key                  = "mock"
  secret_key                  = "mock"
  skip_credentials_validation = true
  skip_metadata_api_check     = true
  skip_requesting_account_id  = true
}
